version: '3.8'

services:
  db:
    image: mysql:8.0
    container_name: desi2026-mysql
    restart: unless-stopped
    environment:
      MYSQL_ROOT_PASSWORD: tsti
      MYSQL_DATABASE: desi2026
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
